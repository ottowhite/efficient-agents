# TODO:
# - Use a lightweight Python base image
# - Install only necessary dependencies
# - Copy only required files
# - Set up environment for agent_server.py
# - Set entrypoint to run agent_server.py

FROM python:3.10-slim

# Set working directory
WORKDIR /app

# Install system dependencies (if any)
RUN apt-get update && apt-get install -y --no-install-recommends \
	build-essential \
	&& rm -rf /var/lib/apt/lists/*

# Copy only requirements first for better caching
COPY requirements.txt .

# Install Python dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Copy the rest of the application code
COPY .env .env

# Expose the port used by agent_server.py
EXPOSE 5000

# Set environment variables (if needed)
ENV PYTHONUNBUFFERED=1

# Run the agent_server.py
CMD ["uvicorn", "--host", "0.0.0.0", "--port", "5000", "--workers", "15", "--loop", "uvloop", "src.examples.beam_search.agent_server:app"]
